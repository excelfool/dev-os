// `server-only` throws when resolved outside a Server Component. Tests exercise
// these modules directly, so the marker is stubbed out here. The guard still
// applies to every real build — this alias is scoped to vitest.
export {};
